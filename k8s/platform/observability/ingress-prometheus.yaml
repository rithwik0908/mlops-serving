# Optional public UI for Prometheus (same TLS Secret as Grafana). Regenerate cert SAN to include this host.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: monitoring
  labels:
    app: prometheus
spec:
  ingressClassName: traefik
  tls:
    - secretName: chameleon-nip-tls
      hosts:
        - prometheus.FLOATING_IP_PLACEHOLDER.nip.io
  rules:
    - host: prometheus.FLOATING_IP_PLACEHOLDER.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus
                port:
                  number: 9090
