# Core serving (always required)
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
numpy==1.26.4
prometheus-client==0.20.0
mlflow>=2.22.0,<3
boto3>=1.34.0,<2

# DUMMY_MODE=false needs torch + transformers — installed in Dockerfile (CPU wheels).
# onnxruntime: optional for SERVING_BACKEND=onnx (see model_onnx.py)
# onnxruntime==1.18.0
