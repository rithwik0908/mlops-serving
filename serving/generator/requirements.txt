# Core serving (always required)
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
numpy==1.26.4
prometheus-client==0.20.0
httpx==0.27.0
mlflow>=2.22.0,<3
boto3>=1.34.0,<2

# Real inference (DUMMY_MODE=false): install from requirements-llm.txt
# (torch, transformers, accelerate, peft for causal+LoRA, better-profanity).
# The generator Dockerfile installs requirements-llm.txt by default (see INSTALL_LLM_DEPS).
