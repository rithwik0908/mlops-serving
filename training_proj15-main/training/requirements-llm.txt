# Small LLM fine-tuning on CPU (generator: formal / friendly / neutral rewrites)
# Install PyTorch CPU wheels first if needed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
transformers>=4.44.0,<5
datasets>=2.18.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.9.0,<0.20
mlflow>=2.10.0,<3
boto3>=1.34.0,<2
PyYAML>=6.0.1
sentencepiece>=0.2.0
