# Training stack aligned with ECE-GY 9183 initial implementation (MLflow + config-driven runs)
# CPU PyTorch: install before this file in Docker (see training/Dockerfile). Local: https://pytorch.org
scikit-learn>=1.3.0,<1.7
# MLflow 3.x clients talk to a different API surface than 2.x servers — pin below 3.
mlflow>=2.22.0,<3
boto3>=1.34.0,<2
PyYAML>=6.0.1
numpy>=1.24.0
scipy>=1.10.0
optuna>=3.5.0,<4
transformers>=4.40.0,<5
datasets>=2.18.0,<4
accelerate>=0.33.0,<2
